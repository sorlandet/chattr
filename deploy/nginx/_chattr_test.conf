upstream chattr-test_server {
    server  127.0.0.1:9000 fail_timeout=0;
}

# Redirect all requests on the www subdomain to the root domain
#server {
#    listen      80;
#    server_name test.wheel-size.com;
#    rewrite ^/(.*) http://beta.wheel-size.com/$1 permanent;
#}

# Serve static files and redirect any other request to Apache
server {
    listen       80;
    server_name  chat.wheel-size.com;
    root        /opt/www/private/chattr;
    access_log  /var/log/chattr-test/nginx/access.log;
    error_log  /var/log/chattr-test/nginx/error.log;

    # Close server from access (test version) django - 159753
    #auth_basic "Restricted";
    #auth_basic_user_file /opt/webapps/.htpasswd;

    ## Only allow these request methods ##
    #if ($request_method !~ ^(GET|HEAD|POST)$ ) {
    #    return 444;
    #}
    ## Do not accept DELETE, SEARCH and other methods ##

    ## Deny certain Referers ###
    #if ( $http_referer ~* (babes|forsale|girl|jewelry|love|nudit|organic|poker|porn|sex|teen) ) {
    #    # return 404;
    #    return 403;
    #}
    ##

    # Stop deep linking or hot linking
    location ^~ /public {
        access_log off;
        expires 7d;
#        valid_referers none blocked finlay.dev.thumbtack.net;
#        if ($invalid_referer) {
#            return 403;
#        }
    }

#    location ^~ /media {
#        access_log off;
#        expires 7d;
#        valid_referers none blocked finlay.dev.thumbtack.net;
#        if ($invalid_referer) {
#            return 403;
#        }
#    }


    try_files $uri @proxy_to_app;

    # Setup named location for Django requests and handle proxy details
    location @proxy_to_app {
        proxy_pass         http://chattr-test_server;
        proxy_redirect     off;
        proxy_set_header   Host             $host;
        proxy_set_header   X-Real-IP        $remote_addr;
        proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
    }
}